import styled from "styled-components";

const HabitChecked = styled.div`
  background-image: url(checked-small.png);
  width: fit-content;
  background-repeat: no-repeat;
  background-size: 80% 80%;
  background-position: center;
  padding: 1rem;
  line-height: 0.2rem;
  margin: auto;
`;

export default HabitChecked;
